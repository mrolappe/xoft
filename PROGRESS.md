# Progress

Overall state of the xoft MVP. One line per milestone; details in the per-phase files.

| Milestone | State | Detail |
|---|---|---|
| M0 — Foundations | ✅ done | [docs/progress/m0-foundations.md](docs/progress/m0-foundations.md) |
| M1 — Grammar | ✅ done (M1.1, M1.2a, M1.2b, M1.2c, M1.3, M1.5 done; M1.4 sweep tool + `INLINE` + bracket pragmas + brace annotations + `ASSEMBLER` + `POINTER TO ARRAY OF Type` + single-quoted strings + bodiless procedure headings + `CASE...ELSE` + Oberon-A system flags/square-bracket library calls + repeated/interleaved decl sections + Oberon-A assignable-procedure mark + STJ-Oberon `AND`/`NOT` keyword operators + AmigaOberon `CLOSE` section/typed sets/real-range lexer fix/unsigned-hex/curly external names/param varargs + AmigaOberon hardware-address vars/D-E scale-factor bug/designator-actual_params ambiguity/NBSP whitespace + external-scanner NBSP/comment fix/voc external-C-procedure decl + oberon-a adjacent-string concatenation/nested-bodiless-decl GLR blowup/underscore identifiers/module external-name + STJ record-embedded procedure headings/trap-bound procedures/assignable nested procedures/`RETURN^`/assembler-comment scanner fix/CASE label widened to ConstExpr/assignment expressions + AmigaOberon `STRUCT`/`UNTRACED POINTER`/`BPOINTER` + module-level decl/procedure interleaving + fixed-length array formal param done, `stj` at 100%, **≥95% exit criterion met — 96.72% (766/792)**; declared done round 26, remaining 26 failures are all scoped out — stubs/one-off corpus artifacts/conditional-compilation preprocessing, see NEXT.md history) | [docs/progress/m1-grammar.md](docs/progress/m1-grammar.md) |
| M2 — Core: lossless parse/serialize | ✅ done (M2.1 codec + grammar linkage, M2.2 token-walk serializer/byte-coverage assertion, M2.3 `strip_comments`, M2.4 rule registry — empty shape, all TDD; full-corpus exit measurement is the one item still awaiting M4's corpus runner) | [docs/progress/m2-core.md](docs/progress/m2-core.md) |
| M3 — Diagnostics and CLI | ✅ done (M3.1 `Diagnostic` + `ERROR`/`MISSING` walk; M3.2 `xoft check`/`xoft transpile` + `codespan-reporting`; M3.3 8 broken-source fixtures + `insta` snapshots, one new `error_message` table entry surfaced by the fixtures — all TDD) | [docs/progress/m3-diagnostics-cli.md](docs/progress/m3-diagnostics-cli.md) |
| M4 — Corpus runner | ✅ done (M4.1 `xoft corpus run` + `corpus/allowlist.toml` — 766/792 counted, 100% parse, 100% round-trip, 26 allowlisted (3.28%, under the 5% D8 cap); M4.2 `.github/workflows/ci.yml` + vendored `corpus/fixtures/` subset (`voc` + `oberon-a`, 12 files) since the real corpus is machine-local-only) | [docs/progress/m4-corpus-runner.md](docs/progress/m4-corpus-runner.md) |
| M5 — Toy dialect Oberon-X | ✅ done (M5.1 grammar fork + `BEGIN`/`DO` synonym + `UNLESS`, 89/89; M5.2 `xoft-core/src/mapping.rs` mapping rules + `serialize::walk_with` emit path, 10 tests; M5.3 6 golden-file pairs under `corpus/cases/` + `xoft-cli/tests/mapping_golden.rs`, 1 test. **Exit measured**: an additive construct (`UNLESS`) round-trips byte-identically both directions; an alias (`DO`/`BEGIN`) is one-way and `X→2→X` only normalizes — injectivity, not code size, predicts the cost) | [docs/progress/m5-oberon-x.md](docs/progress/m5-oberon-x.md) |
| M6 — Testbed | ✅ done (M6.1 Tauri shell; M6.2 Vite + Monaco `DiffEditor` frontend, security finding from M6.1 resolved at the IPC boundary; M6.3 web-tree-sitter semantic-token highlighting + positioned diagnostics with bidirectional click navigation. Manual verification round 41 found four real bugs; round 42 fixed all four — `manifest::build` now collects per-root failures instead of aborting, the source pane is editable (`originalEditable: true`), the window background is opaque, and semantic-token coloring renders (root cause: `semanticHighlighting.enabled` must be set at `createDiffEditor` construction time, not via a later `updateOptions()` call). All four re-verified in a real `cargo tauri dev` window) | [docs/progress/m6-testbed.md](docs/progress/m6-testbed.md) |
| M7 — Phase 2 plan | ⬜ not started | — |

**Next task:** see [NEXT.md](NEXT.md).

Cross-cutting records: [insights](docs/insights.md) · [errors and mitigations](docs/errors.md) ·
[plan and decisions](docs/plan.md) · [language baseline](docs/language-baseline.md)

## Rounds

| # | Date | Covered |
|---|---|---|
| 1 | 2026-08-10 | Project bootstrap, M0 complete |
| 2 | 2026-08-10 | M1.1 base grammar vendored and building; M1.5 highlights.scm |
| 3 | 2026-08-10 | M1.2a — receivers, forward declarations, DEFINITION module header |
| 4 | 2026-08-10 | M1.2b — WITH, LOOP, EXIT, RETURN statements, empty statements, CASE label ranges confirmed |
| 5 | 2026-08-10 | M1.2c — IS, SET, open arrays confirmed as already working; procedure types fixed in `formal_type`; incidental fix for `field_list_seq` trailing-semicolon gap |
| 6 | 2026-08-10 | M1.3 — external C scanner for nested comments + `(*$…*)` pragma node; `.gitignore` fix so `scanner.c` is tracked |
| 7 | 2026-08-10 | Infra: generated tree-sitter output moved to `gen-src/` (gitignored), `src/` now holds only hand-written `scanner.c` plus symlinks into `gen-src/` so `tree-sitter test`/`parse` keep working unmodified |
| 8 | 2026-08-10 | M1.4 — corpus sweep script (`sweep_corpus.py`), first honest full-corpus number (15.78% → 21.97%); fixed `ELSIF`/`ELSEIF` keyword typo, hex-integer-literal token bug (this is what actually blocked `INLINE`, which needed no new grammar), two AmigaOberon `IMPORT` rename/re-export variants; triaged (not fixed) bracket pragmas (212 files), `STRUCT`, brace-annotated procedures, `ASSEMBLER` |
| 9 | 2026-08-10 | M1.4 continued — scoping decision flagged to and resolved by user (`STRUCT` deferred to Phase 2, `ASSEMBLER` deferred pending scanner work); implemented `<* ... *>` bracket pragmas (third external scanner token) and AmigaOberon brace annotations (`vector_offset`, `param_offset`); 21.97% → 27.15% (174 → 215/792) |
| 10 | 2026-08-10 | M1.4 continued — implemented `ASSEMBLER` blocks (fourth external scanner token, word-boundary raw-scan to `END`); 27.15% → 29.29% (215 → 232/792); confirmed the two carried-over items (`POINTER TO ARRAY OF Type`, single-quoted char literals) as real `ERROR`-causing gaps, not yet implemented |
| 11 | 2026-08-10 | M1.4 continued — implemented `POINTER TO ARRAY OF Type` (length made optional in `array_type`) and single-quoted strings (widened `string_literal`, not a separate `CHAR` type — round 10's "no single-quote form in the report" claim was wrong, corrected against `docs/language-baseline.md` and corpus FourCC evidence); 29.29% → 30.68% (232 → 243/792); triage table from rounds 9/10 now fully resolved |
| 12 | 2026-08-10 | M1.4 continued — implemented AmigaOberon's bodiless procedure heading (`Interfaces/*.mod` system-call wrappers), reusing `definition_proc_decl` as a third `procedure_decls` alternative and adding a `conflicts` declaration for the resulting GLR ambiguity with `procedure_decl`; 30.68% → 36.36% (243 → 288/792) |
| 13 | 2026-08-10 | M1.4 continued — implemented `CASE ... ELSE ... END`, a normative Oberon-2 EBNF construct the grammar was simply missing (not a dialect scoping question); `case_statement` gained the same optional `ELSE` arm `if_statement` already had; 36.36% → 39.39% (288 → 312/792) |
| 14 | 2026-08-10 | M1.4 continued — implemented Oberon-A's square-bracket dialect family from `Oberon-A/docs/OC.doc`: `sysflag` on `MODULE`/`POINTER`/`RECORD`/`PROCEDURE`, `square_vector_offset` and `external_code_names` on procedure headings, `reg_spec` (with vararg `..` marker) on formal parameters; 39.39% → 41.41% (312 → 328/792) |
| 15 | 2026-08-10 | M1.4 continued — fixed `DeclSeq` to allow repeated/interleaved `CONST`/`TYPE`/`VAR` sections per the normative baseline EBNF's outer `{}` (was fixed-order, one of each); a plain grammar bug, not a dialect extension; 41.41% → 54.42% (328 → 431/792), +103 files, largest single-round gain to date |
| 16 | 2026-08-10 | M1.4 continued — implemented Oberon-A's "assignable procedure" mark (`PROCEDURE* [sysflag] ident`, `*` right after the `PROCEDURE` keyword, per `docs/OC.doc` "AssignableProcs"); reused the existing `kStar` token, no scanner change; 54.42% → 60.61% (431 → 480/792), +49 files |
| 17 | 2026-08-10 | M1.4 continued — first sampling pass over `stj` (Atari ST Oberon); implemented `AND`/`NOT` as textual synonyms for `&`/`~` (STJ dialect extension, confirmed via corpus grep and the compiler's own embedded keyword table); two new keyword tokens, no scanner change; 60.61% → 66.41% (480 → 526/792), +46 files, all in `stj` |
| 18 | 2026-08-10 | M1.4 continued — first sampling pass over `amiga-oberon-31` since round 12; six fixes: module-level `CLOSE` finalizer section, `LONGSET`/`SHORTSET` typed set constructors, a cross-dialect real-number/range lexer bug (`2..4` mis-tokenized as `2.` + `.4`), unsigned-hex `U` integer suffix, curly-brace `external_code_names` sibling, `param_offset` varargs `..` marker; 66.41% → 69.95% (526 → 554/792), +28 files |
| 19 | 2026-08-11 | M1.4 continued — implemented `Sparks.mod`'s hardware-address var annotation (`addressed_ident`); found and fixed two more bugs while sampling: `D`/`E` scale-factor literal (cross-dialect, +26 files) and the `designator`/`actual_params` type-guard-vs-call ambiguity (moved `actual_params` into `designator`'s own repeat alongside `selector`, cross-dialect, +35 files, largest single-fix gain to date); plus NBSP-as-whitespace fix; 69.95% → 78.16% (554 → 619/792), +65 files total |
| 20 | 2026-08-11 | M1.4 continued — fixed round 19's NBSP+comment GLR lead: the external scanner's own `is_space()` (used to skip leading whitespace before checking for a comment) didn't recognize NBSP, unlike `extras`' regex, so a comment right after an NBSP made the external scan decline and desync the `procedure_decl`/`definition_proc_decl` GLR fork (+3 files: `Lists.mod`, `FArrays.mod`, `linkedlists.mod`); first dedicated `voc` sampling pass found and implemented voc's bodiless "external C procedure" heading (`PROCEDURE -ident(...) "C code";`, 56 corpus occurrences, new `external_proc_decl` rule, +6 files); user chose to defer `Break.mod`/`NoGuru.mod`'s dual pragma-guarded `MODULE` headers (2 files, structural) rather than implement or scope out this round; 78.16% → 79.29% (619 → 628/792), +9 files. Remaining `voc` failures (4) are two trailing-garbage files (free text / binary blob appended after `END Module.`) and one newly-found bare-real-literal lexer ambiguity (`1.` colliding with round 18's `2..4` range fix) needing external-scanner lookahead — not attempted this round, see NEXT.md |
| 21 | 2026-08-11 | M1.4 continued — `oberon-a`'s first full sampling pass (59 failures, never sampled before): adjacent string-literal concatenation (C-style, +13 files); root-caused and fixed an exponential GLR blowup on 8+ consecutive bodyless procedure headings — `procedure_body`'s nested-declaration repeat wrongly reused the *module*-level `procedure_decls` (including bodyless variants with no `END` to anchor GLR's nesting search), narrowed to just `procedure_decl`/`forward_decl` per the baseline `DeclSeq` EBNF (+10 files); underscore missing from the identifier continuation set, so `TYPE_HGROUP` lexed as keyword `TYPE` + garbage (+10 files, cross-checked prevalence: 72 oberon-a/35 amiga-oberon-31/13 voc files use it); module-level external object-file name reusing `external_code_names` (+5 files). 79.29% → 84.09% (628 → 666/792), +38 files, largest single-round gain since round 15. Found but did not attempt a genuine dialect ambiguity: `ErrorMessages.mod`/`OBumpRevMsg.mod` need `\"` to escape a string's closing quote, but `voc`'s `ulmPrint.Mod`/`Printer.Mod` already rely on `"\"` being a complete one-backslash string with no escape processing — the two readings directly conflict, flagged to the user rather than guessed. All four fixes stayed within `oberon-a`; `amiga-oberon-31`/`stj`/`voc` unchanged, see NEXT.md |
| 22 | 2026-08-11 | M1.4 continued — `stj`'s first full sampling pass since round 17 (40 failures): DEFINITION-module `RECORD` bodies listing type-bound procedure headings as field-list items (+11 files); `PROCEDURE-` GEMDOS/BIOS/XBIOS trap-bound headings with a trailing `trap,function` integer pair, needing a `conflicts` entry against voc's `external_proc_decl` (+9 files); `PROCEDURE~` assignable nested-procedure mark and `RETURN^` non-local return, both confirmed against `stj`'s own compiler manual `DOC/STJ-OBN.TXT` (+15 files, biggest single fix); `ASSEMBLER` block scanner not skipping `;`-style comments (falsely terminated on comment prose containing "END"); CASE label widened from `integer|string|qualident` to the baseline's actual `ConstExpr`; assignment expressions (`(x := f()) > 0`, `a := b := proc()`), also confirmed against `DOC/STJ-OBN.TXT` (last three combined, +6 files). 84.09% → 89.27% (666 → 707/792); `stj` reached **0 failures**, the first root to fully pass. One cross-dialect bonus fix (`oberon-a`'s `OCS.mod`, caught by the by-round re-tally check); `amiga-oberon-31`/`voc` unchanged. See NEXT.md for the three still-open scoping questions (`Break.mod`/`NoGuru.mod`, `OberonLib.mod`, `\"` string-escape) now blocking further progress on the other roots |
| 23 | 2026-08-11 | M1.4 continued — user asked what implementing round 9's deferred `STRUCT`/`UNTRACED POINTER` would actually entail; sampling the real corpus (not re-trusting round 9's "bigger than lexical-superset scope" note) showed it's structurally near-identical to `RECORD`/`POINTER`, so implemented it: `STRUCT` (near-`RECORD` shape, parenthesized slot is an embedded named field not a bare base type, C-struct-embedding style, joins `struct_type`'s choice since it also appears as an anonymous pointer target), `UNTRACED POINTER` (new optional keyword ahead of `kPointer`), and `BPOINTER` (found mid-round while re-tallying failures — AmigaDOS's BCPL-relative pointer, replaces `POINTER` entirely rather than modifying it; first attempt modeled it as a modifier like `UNTRACED` and was wrong, caught before `--update` by re-reading the corpus line). 89.27% → 95.96% (707 → 760/792), +53 files. **M1's ≥95% exit criterion is now met.** User also resolved two of the three open scoping questions: dual pragma-guarded `MODULE` headers stay out of M1 (Phase 2); the `\"`/`"\"` string-escape conflict is to be made dialect-specific rather than picking one reading (not yet implemented). `stj` unchanged at 0; `amiga-oberon-31` 61 → 8 (3 still-deferred pragma-header files + 5 newly-visible, undiagnosed whole-file-`ERROR` failures); `oberon-a`/`voc` unchanged |
| 24 | 2026-08-11 | M1.4 continued — user chose to keep closing gaps past the exit criterion; diagnosed round 23's 5 undiagnosed `amiga-oberon-31` failures and found they were unrelated to that round's `STRUCT`/`UNTRACED`/`BPOINTER` theme: (1) `module`'s decl/procedure structure only allowed one `(CONST|TYPE|VAR)*` block followed by one `PROCEDURE*` block, but this dialect interleaves them repeatedly — merged into a single `repeat(choice(...))` over all four, a strict superset (+4 files); (2) `GarbageCollector.mod` also hit an unrelated, corpus-wide-unique defect — `formal_type`'s `ARRAY OF` shorthand had no room for a length, needed for one fixed-length by-reference array parameter — widened to `optional($.length)` (+1 file). Both fixes dialect-neutral, not gated to `amiga-oberon-31`. 95.96% → 96.72% (760 → 766/792), +6 files. `stj` unchanged at 0; `amiga-oberon-31` 8 → 3 (now exactly the deferred dual pragma-header files); `oberon-a` 20 → 19 (one file fixed as a side effect, category not yet re-triaged); `voc` unchanged at 4 |
| 25 | 2026-08-11 | M1.4 continued — full re-triage of `oberon-a`'s remaining 19 failures: all trace to already-scoped-out categories (8 stub files, 3 known-deferred items, 5 one-off single-stray-byte corpus artifacts unique to their own file each, 1 malformed AmigaGuide preamble file), plus confirmation that 4 files (`Kernel.mod`, `IntuiPointerDemo.mod`, `amiga/Utility.mod`, and `amiga-oberon-31`'s already-deferred `Break.mod`/`NoGuru.mod`) are all the same real feature — Amiga Oberon's conditional-compilation preprocessor (`$IF`/`$ELSE`/`$END`, in either comment-embedded or bracket-pragma surface syntax) — no single Oberon-2 parse tree can represent both unconditionally-present branches. No new grammar gap found; user confirmed declaring `oberon-a` done rather than implementing preprocessing or chasing one-off bytes. **Attempted and reverted a NUL-byte `extras` fix that hung the entire parser** (NUL is tree-sitter's own EOF sentinel) — caught before committing, `git diff` confirmed zero change landed, `tree-sitter test` clean 85/85 after revert. 96.72% (766/792) unchanged; no grammar/scanner diff this round |
| 26 | 2026-08-11 | **M1 declared done** (766/792, 96.72%, exit criterion met since round 23; user chose "start M2" over scoping conditional-compilation preprocessing, after being walked through why the latter conflicts with D4/D8's byte-identical round-trip guarantee). M2.1 `codec.rs` (`Document` byte↔char bijection, D3) implemented TDD, 3 tests. New: linked the vendored grammar into Rust for the first time — `build.rs` compiles `gen-src/parser.c`+`src/scanner.c` via `cc` directly into `xoft-core`, `grammar.rs` exposes `tree_sitter::Language`. M2.2 token-walk serializer + byte-coverage assertion (D4) in `serialize.rs`, TDD, 4 tests including a full codec→parse→walk→reconstruct→codec byte-identical round trip with a high byte in a comment. Found (empirically, before designing the walk) that tree-sitter's `extras` still produce real leaf nodes — comments are leaves, not invisible — so gaps hold only whitespace with no special-casing. Ad hoc, uncommitted 240-file corpus sample (60/root) confirmed the serializer's `rt_ok` is `true` on every file including ones with M1-known `ERROR` nodes — serializer correctness is orthogonal to M1's parse coverage, as expected. M2.3/M2.4 and the full-corpus exit measurement (needs M4's corpus runner) not started |
| 27 | 2026-08-11 | M2.3 `strip_comments` implemented TDD, 5 tests. Confirmed via `codegraph_explore` that `pragma`/`bracket_pragma` are distinct tree-sitter node kinds from plain `comment` (not text-sniffed), so stripping is a one-line kind filter over the same leaf list M2.2's `walk` already builds (`collect_leaves` widened to `pub(crate)` and reused, no duplicated traversal). One design decision beyond the plan's literal wording: a removed comment's bytes are replaced with a single space rather than deleted outright, since a comment can be the only separator between two tokens (e.g. `THEN(*c*)y`) and outright deletion would fuse them into one identifier, breaking the "output must re-parse" contract — caught by a dedicated test before it could ship silently wrong. `cargo test --workspace` green, 25 tests in `xoft-core`. M2.4 (empty rule-registry shape) is the only M2 item left |
| 28 | 2026-08-11 | User resolved M2.4's open design question: defer it until after M3.1, so the registry can be typed against a real `Diagnostic` instead of a placeholder — re-ordered M3.1 ahead of M2.4. Then resolved M3.1's own gap the same way: `docs/plan.md` says a `MISSING` node's kind *is* the message but never defines `ERROR`'s "context-based message upgrading," so asked before coding rather than guessing; user chose a small lookup table keyed on the `ERROR` node's immediate parent kind, generic fallback otherwise. Implemented `diagnostic.rs` (`Diagnostic { start_byte, end_byte, message }`, `diagnostics(tree) -> Vec<Diagnostic>`) TDD, 4 tests. Probed the real parser first (throwaway `tests/_scratch_probe.rs`, deleted before commit) rather than hand-deriving expected tree shapes: found a missing `;` produces an `ERROR` node (parent `"assignment"`, the table's one grounded entry) rather than a `MISSING ";"`, and that an unbalanced `(` does produce a genuine zero-width `MISSING ")"` but `tree-sitter parse`'s default pretty-printer doesn't render it inline — only `Node::is_missing()` sees it reliably. `cargo test --workspace` green, 29 tests in `xoft-core`. M2.4 remains the only M2 item left, now unblocked; M3.2/M3.3 not started |
| 29 | 2026-08-11 | M2.4 rule registry (empty shape, Phase 1) implemented TDD, 2 tests. Asked the user the one open design question first (does `Rule::check` need `text: &str` alongside `tree: &Tree`): confirmed yes, a node only carries byte spans and adding the parameter later would break every future rule. `crates/xoft-core/src/rule.rs`: `Rule` trait (`check(&self, tree, text) -> Vec<Diagnostic>`), `RuleRegistry` (`new`/`register`/`run`, flat-maps `check` over its `Vec<Box<dyn Rule>>`). Tests prove only the wiring — an empty registry returns `[]`, a trivial always-flags-root rule actually gets invoked and its diagnostic collected — no real rule exists until M5. `cargo test --workspace` green, 31 tests in `xoft-core`. **M2 declared done** (its only remaining sub-item, the full-corpus exit measurement, explicitly waits on M4's corpus runner regardless). Next: M3.2 (`xoft transpile`/`xoft check` + `codespan-reporting`) |
| 30 | 2026-08-11 | M3.2 `xoft check`/`xoft transpile` + `codespan-reporting` implemented TDD, 4 new tests. Asked the user the one open scoping question first (`NEXT.md`'s flagged ambiguity: what should Phase-1 `transpile` do before M5's dialect rules exist): confirmed `check` + a lossless round-trip through M2's serializer. New `xoft-cli` library modules (I/O-facing, `xoft-core` untouched): `check.rs` (`check_source`/`check_file`, merges `diagnostic::diagnostics` + empty `RuleRegistry::run`, renders via `codespan-reporting`'s `SimpleFiles`/`term::emit`) and `transpile.rs` (`transpile_file`, adds `serialize::walk`+`reconstruct` round-trip on top of `check`). `main.rs` gained `Check`/`Transpile` subcommands, siblings of `Corpus`; both exit 1 on non-empty diagnostics. `codespan-reporting = "0.12"` is the workspace's first use of this dependency, confined to `xoft-cli`. One test-writing miss caught by the red/green cycle itself, not by review: assumed the location-marker glyph was `-->`, real output uses `┌─` — fixed before commit, no process gap worth a checklist entry. `cargo test --workspace` green, 8 tests in `xoft-cli` (4 → 8) + 31 unchanged in `xoft-core`. M3.3 (broken-source fixtures + `insta` snapshots) is the only M3 item left |
| 31 | 2026-08-11 | M3.3 8 broken-source fixtures (`crates/xoft-cli/tests/fixtures/broken/*.mod`) + `insta` snapshots over `check_source`'s rendered output (`crates/xoft-cli/tests/broken_fixtures.rs`), one parametrized test asserting structural facts (diagnostic count/message) against `CheckResult::diagnostics` plus the snapshot for rendered text, per round 30's data-vs-presentation insight. Every fixture's real parse shape probed first via a throwaway `xoft-core/tests/_scratch_probe.rs` (deleted before commit) — several first-draft sources didn't reproduce their intended failure once actually parsed (a nested `BEGIN` inside `IF...THEN` isn't valid Oberon; a binary-expression right-hand side changed which parent kind a missing-`;` recovery lands on; getting a *second* independent diagnostic in one file took 5 source variants since this grammar's `ERROR`-node recovery swallows everything to the file's end once triggered, only clean `MISSING`-token insertion stays localized) — none guessed from memory. `error_message` (`xoft-core/src/diagnostic.rs`) gained one new grounded entry, `parent.kind() == "module"` → `"unexpected token in module body"`, covering two distinct real mistakes (a stray keyword, an extra `END`) that land on the same parent kind — table stays keyed on parent kind only, not widened to inspect the `ERROR` node's own children for a fixture-specific message. Caught one bug before the first snapshot was accepted: `check_file` would have baked the absolute checkout path into every committed `.snap`; switched fixtures to `check_source(case.file, ...)` with the bare filename. `insta = "1.48.0"` added dev-only to `xoft-cli`, snapshots accepted via `INSTA_UPDATE=always` (confirmed red first, on the missing table entry, then green in one pass). `cargo test --workspace` green, `xoft-cli` 8 → 9, `xoft-core` unchanged (28). **M3 declared done** (M3.1 + M3.2 + M3.3 all complete) |
| 32 | 2026-08-11 | M4.1 `xoft corpus run` (`crates/xoft-cli/src/corpus_run.rs`) implemented TDD, 5 new tests — pure `aggregate(allowlist, outcomes) -> CorpusReport` (bucketed failure histogram, per-root breakdown, allowlist-excluded percentages, sorted/deterministic) split from I/O-facing `run(roots, allowlist)`, which reuses `manifest::build`'s file list and a newly-extracted `transpile::transpile_source` rather than re-implementing either. `main.rs` gained `Corpus Run`. Ran it for real against `corpus/roots.toml` (all four roots resolve on this machine): 766/792 parse clean, 792/792 round-trip clean before any allowlisting — every failure is parse-only. Re-verified all 26 failures against actual bytes/diagnostics this round rather than porting M1's stale notes; one, `voc/ulm/ulmRandomGenerators.Mod`'s bare-decimal real literal (`1.`), was newly characterized as a live counter-example to `grammar.js`'s own "no real-world corpus code relies on that" comment — asked the user whether to reopen frozen M1 or allowlist; user chose allowlist. `corpus/allowlist.toml` created, 26/792 (3.28%) entries, under the 5% D8 cap, each with a freshly-grounded one-line reason. **D8's exit criterion now met**: `reports/corpus-report.json` (checked in) shows 100% parse / 100% round-trip on the 766 counted files; confirmed byte-stable across two consecutive runs. `cargo test --workspace` green, `xoft-cli` 9 → 14, `xoft-core` unchanged (28). M4.2 (CI check) is the only M4 item left |
| 33 | 2026-08-11 | M4.2 CI check. Scoping question resolved with the user first (real corpus is machine-local-only, no CI access): vendor a fixture subset instead — `voc` from `github.com/vishaps/voc` (GPL-3.0) and `oberon-a` from its Aminet archive (freely redistributable per its own readme), `amiga-oberon-31` (commercial) and `stj` (no source URL at hand) left out per user's "when in doubt, leave it out." `corpus/fixtures/` (`roots.toml`, `allowlist.toml` empty, `NOTICE.md`, `report.json`), 12 files (6/root), picked clean and directory-diverse, confirmed 100%/100% before committing. `.github/workflows/ci.yml`, new — `cargo test --workspace` + fixture `corpus run` + `git diff --exit-code` on the fixture report. Zero `xoft-core`/`xoft-cli` code changes: M4.1's already-overridable `--roots`/`--allowlist`/`--out` flags were the whole mechanism. **M4 declared done.** Next is M5 (Opus-tagged in `docs/plan.md`) |
| 34 | 2026-08-11 | CI fix: round 33's first-ever fresh-checkout CI run failed on `cargo test --workspace` — `build.rs` compiles `grammars/tree-sitter-oberon2/gen-src/parser.c`, which round 7 made gitignored (generated locally via `tree-sitter generate`, never committed), and CI never ran that generation step. Added `npm install -g tree-sitter-cli` + `tree-sitter generate ... -o gen-src` ahead of `cargo test` in `.github/workflows/ci.yml`. Verified locally by moving `gen-src/` aside to simulate a fresh checkout, running the exact new CI commands, and confirming `cargo test --workspace` passes; restored the original `gen-src/` after. No `xoft-core`/`xoft-cli` code changed. Next task unchanged: M5.1 |
| 35 | 2026-08-18 | M5.1 `grammars/tree-sitter-oberon-x/grammar.js`. Scoping questions resolved with the user first (per `NEXT.md`): fork `tree-sitter-oberon2/` wholesale, no `xoft-core` wiring this round, `BEGIN`/`DO` as a synonym not a breaking rename, fixtures in the grammar's own native `test/corpus/` rather than a new top-level `corpus/cases/`. Forked via `rsync`, dropped `sweep_corpus.py` (real-corpus-only tool, irrelevant to a toy dialect), updated `NOTICE` for the second-order provenance. TDD: 4 new corpus cases (`DO` as `BEGIN` synonym at procedure- and module-level, `UNLESS` with and without a body) written and confirmed red (`ERROR` nodes) against the unmodified fork before implementing; `unless_statement` rule + `kUnless` token added, `choice($.kBegin, $.kDo)` at both sites `kBegin` appeared alone. Two fork-specific traps hit and fixed (see `docs/errors.md` round 35): bare `tree-sitter generate` collides with the copied `src/` symlinks until `gen-src/` exists (`-o gen-src` required), and renaming `grammar.js`'s `name` field requires renaming `scanner.c`'s `tree_sitter_<name>_external_scanner_*` symbols to match or linking fails. Full suite green, 89/89 (85 inherited + 4 new). No `xoft-core`/`xoft-cli` changes — M5.1 stayed confined to `grammars/`, per the resolved scoping question. Next: M5.2 (Opus-tagged mapping rules + emit path) |
| 36 | 2026-08-18 | M5.2 mapping rules + emit path, TDD, 10 new tests. Design settled with the user up front: one rule per M5.1 feature (Rule A `DO`-as-block-opener → `BEGIN`; Rule B `UNLESS E DO S END` ⟷ `IF ~(E) THEN S END`, `~` not `NOT` per `docs/language-baseline.md:112`), a new `crates/xoft-core/src/mapping.rs` rather than bending `rule.rs`'s diagnostic-shaped `Rule` trait into a text-emitting one, and emit by extending M2's `Span` machinery rather than writing a printer. `serialize::walk` generalized into `walk_with(tree, text, emit)` — `emit` sees each leaf and may replace it with any number of spans, gaps are never offered to it, so "inherited indentation" is literally the original bytes carried forward; `walk` is now a one-line delegation and the gap logic lives in one place. Second `tree_sitter::Language` wired (`grammar::language_oberon_x`, C symbol `tree_sitter_oberon_x`); `build.rs` loops over both grammar dirs; `.github/workflows/ci.yml` gained the matching `tree-sitter generate -o gen-src` step, verified by deleting both `gen-src/` dirs and replaying the exact CI commands. **Corrected an impossible premise in the round's own brief**: `X→2→X` byte-identity cannot hold for Rule A, because `BEGIN`/`DO` are synonyms — two Oberon-X spellings collapse onto one Oberon-2 spelling, so the mapping is many-to-one and no reverse rule can recover the original; the symmetric fix (2→X rewriting `BEGIN`→`DO`) only moves the loss onto Oberon-X sources that spell it `BEGIN`. Kept Rule A one-way and asserted the three invariants that actually hold (`2→X→2` byte-identical unconditionally; `X→2→X` byte-identical for Rule B, which *is* a bijection; `X→2→X` byte-identical up to `DO`→`BEGIN` normalization for Rule A). `cargo test --workspace` green, `xoft-core` 28 → 38, `xoft-cli` 14 unchanged, `tree-sitter test` still 89/89. Next: M5.3 |
| 37 | 2026-08-19 | M5.3 golden-file round-trip suite, **M5 done**. Promoted 6 of M5.2's 10 unit-test fixtures to `corpus/cases/<name>.{x,2}.mod` pairs (`do_proc`, `do_module`, `unless_body`, `unless_empty`, `unless_atom`, `comment_gap`) and one table-driven integration test, `crates/xoft-cli/tests/mapping_golden.rs` — placed in `xoft-cli`, not `xoft-core`, since reading fixtures from disk is I/O and `xoft-core` performs none by design; reused the `xoft-cli/tests/broken_fixtures.rs` (M3.3) fixture-table pattern rather than inventing a new one. One `lossy: bool` per case (Rule A vs Rule B) drives all four round-trip assertions instead of a third expected-output file per case. All green on the first run — pure promotion of already-implemented M5.2 behavior to file-driven coverage, no red phase. Wrote M5's exit write-up (`docs/progress/m5-oberon-x.md`): the measured cost driver is injectivity, not feature size — additive constructs (`UNLESS`) round-trip byte-identically both directions, aliases (`DO`/`BEGIN`) are one-way and `X→2→X` only normalizes. `cargo test --workspace` green, `xoft-cli` 14 → 15, `xoft-core` unchanged at 38. Next: M6.1 (Tauri shell) |
| 38 | 2026-08-23 | M6.1 Tauri shell. Resolved three `NEXT.md` scoping questions with the user first: `transpile` means the Oberon-X/Oberon-2 dialect mapping (not the CLI's check+round-trip `transpile`); crate layout split (`crates/xoft-testbed/` backend, top-level `testbed-ui/` frontend); CI wiring deferred. New workspace member `crates/xoft-testbed/`, scaffolded via `cargo tauri init` then renamed/reworked to match this repo's conventions (`version.workspace = true`, unused `log`/`tauri-plugin-log` scaffold deps dropped). `commands.rs` holds the three command bodies as plain, `tauri`-free functions (TDD, written against a nonexistent module first, all 5 tests green on the first real run): `list_corpus` (thin wrapper over `xoft_cli::manifest::build`), `roundtrip_check` (reuses `corpus_run.rs`'s `parse_ok`/`round_trip_ok` computation via `transpile_source`, not reimplemented), `transpile` (picks `language`/`language_oberon_x` and `mapping::to_oberon2`/`to_oberon_x` together via a `Direction` enum so grammar and mapping direction can't drift apart). `xoft_core::diagnostic::Diagnostic` gained `Serialize` (one line) to cross the IPC boundary. `lib.rs` wraps these as `#[tauri::command]`s; `testbed-ui/index.html` is a single static page (no Vite/npm yet, that's M6.2) with `withGlobalTauri: true` so it can call `invoke` directly, writing results via `.textContent` (never `.innerHTML`). Verified `Direction`'s exact serde kebab-case names in a throwaway test before wiring the frontend's `<select>` values to them, deleted after. `cargo test --workspace` green: `xoft-core` 38, `xoft-cli` 15 (both unchanged), `xoft-testbed` 5 (new); `cargo build -p xoft-testbed` links the Tauri binary; `clippy` clean. Security review (this round's diff) surfaced one finding — `list_corpus` walks any filesystem path the webview supplies, unscoped — **acknowledged and documented, not fixed**: current frontend is fully static/first-party so nothing untrusted can reach it yet, and a real fix (Tauri fs-capability scoping) fits better once M6.2 defines what the real frontend sends; see `docs/progress/m6-testbed.md`. Not verified: an actual `cargo tauri dev` window (no display server in this environment) — command bodies are tested directly instead. Next: M6.2 (Vite + Monaco `DiffEditor`) |
| 39 | 2026-08-23 | M6.2 Vite + Monaco `DiffEditor` frontend. Resolved four `NEXT.md` scoping questions with the user first (layout: minimal + corpus file picker; diagnostics: plain list, byte→line/column conversion stays M6.3; `rootsToml` input; security: resolve M6.1's acknowledged finding this round). New backend command `read_corpus_file` (path-traversal-safe, canonicalize+`starts_with`, TDD) fills a real gap M6.1 didn't have — no prior command read a file's actual content. Own security review caught that `read_corpus_file`'s first draft (mirroring `list_corpus`'s existing `roots_toml` parameter, per the approved plan) reproduced M6.1's acknowledged trust-boundary finding on the new command too; fixed by moving the boundary to the `#[tauri::command]` wrappers themselves, which now read `corpus/roots.toml` from disk directly rather than accepting it over IPC — `list_corpus`/`read_corpus_file` are zero/two-argument commands from the frontend's perspective. `testbed-ui/` gained real Vite tooling from nothing: one `monaco.editor.createDiffEditor` doubles as the editable source pane and the diff view, diagnostics as a plain list, corpus sidebar wired to `list_corpus`/`read_corpus_file`, all DOM writes via `textContent`. `cargo test --workspace` green (`xoft-testbed` 5 → 8), `cargo clippy` clean, `npx tsc --noEmit`/`npm run build` clean. Not verified: `cargo tauri dev` in a real window (no display server in this environment, same M6.1 limitation). Next: M6.3 (`web-tree-sitter` highlighting, clickable diagnostics) |
| 40 | 2026-08-23 | M6.3 `web-tree-sitter` highlighting, `ERROR` nodes marked, clickable diagnostics. **M6 declared done.** Resolved four `NEXT.md` scoping questions with the user first (full semantic-tokens scope for both grammars; byte→position conversion in Rust; bidirectional list↔editor click navigation; checked-in `.wasm` artifacts). New `xoft_core::position::byte_to_position` (pure, TDD, 6 tests) plus a testbed-local `PositionedDiagnostic` wrapper (`xoft-testbed/src/commands.rs`) — `xoft_core::diagnostic::Diagnostic` itself stays byte-only since the CLI's `codespan-reporting` rendering needs bytes, not positions; the position concern is layered only at the IPC boundary that needs it. A first-draft integration test hand-guessed a diagnostic's line by analogy with an old round's note; running it red-first caught the guess was wrong (checklist-flagged pitfall), fixed against the real probed value. Compiled both grammars to WASM (`tree-sitter build --wasm`, tree-sitter-cli 0.26's bundled wasi-sdk, no Emscripten needed) and checked them in under `testbed-ui/src/grammars/` (`!testbed-ui/src/grammars/*.wasm` exception added to the repo's blanket `*.wasm` `.gitignore`), regenerated by a new `testbed-ui/scripts/build-wasm-grammars.sh`. New `testbed-ui/src/highlighting.ts`: a Monaco semantic-tokens provider per dialect, parsing the live model with `web-tree-sitter` and reusing the grammar's own existing `highlights.scm` query (not a second hand-rolled classifier) via `Query.captures`; overlapping/nested captures resolved with an innermost-capture-wins walk (map captured node ids, then each leaf walks up to its nearest captured ancestor) rather than porting tree-sitter's own highlight-priority algorithm. Verified the parsing+query layer directly against both compiled grammars with a throwaway Node script (deleted before commit) before wiring Monaco — found and documented (not fixed, out of scope) two pre-existing `highlights.scm` coverage gaps: `kUnless` has no capture rule, and bare-identifier expression reads aren't captured as `@variable`. `main.ts` diagnostics now flow through `monaco.editor.setModelMarkers` (native squiggly+hover) and support both click directions (list→editor via `revealRangeInCenter`/`setSelection`, editor→list via `onMouseDown` + `Range.containsPosition`). `cargo test --workspace` green (`xoft-core` 38 → 44, `xoft-testbed` 8 → 9), `clippy` clean, `tsc --noEmit`/`npm run build` clean. Security review (fresh sub-agent, `security-review` skill): no high-confidence findings. Not verified: `cargo tauri dev` in a real window (no display server, same limitation since M6.1). Next: M7 |
| 41 | 2026-08-26 | Finished M6.3's manual `cargo tauri dev` verification (started, left incomplete round 40). Confirmed working: bidirectional click nav (list→editor and editor→list), tested against a real corpus parse failure (`oberon-a`'s `IntuiPointerDemo.mod`, the known `$IF`/`$ELSE`/`$END` case). Confirmed broken: semantic-token coloring still doesn't render (checked with real keyword/type/string variety, well after the async request would have settled — rules out round 40's "stale screenshot" theory). Found two further real bugs while verifying: `manifest::build` aborts its whole loop on the first unreadable corpus root (one machine-local Nextcloud path gap blanked the entire sidebar, not just that root); the diff editor's "original" pane is not user-editable at all (`originalEditable` left at Monaco's `false` default, so the documented "type directly into the editor" fallback is itself broken — masked because corpus-picker loads bypass the UI gate via `setValue`). Also found, incidentally: the app window has no opaque background and lets whatever's stacked behind it show through — caused a transient, incidental exposure of unrelated on-screen content during this session's screenshots, deleted immediately after review. No source changed (pure verification); `corpus/roots.toml` temporarily edited to work around finding #3, reverted via `git checkout` before ending. See `docs/progress/m6-testbed.md`'s round-41 addendum for full detail and `NEXT.md` for what's next |
| 42 | 2026-08-29 | Closed all four round-41 bugs, test-first throughout. `manifest::build` (`xoft-cli`) now returns `Manifest.failures: Vec<RootFailure>` instead of aborting on the first bad root — split into a per-root `walk_root` helper, signature dropped `Result` (no longer a fallible operation once every root-level error is caught internally), 3 call sites updated; new test `continues_past_an_unreadable_root`. Added Vitest to `testbed-ui` (none existed); new zero-side-effect `testbed-ui/src/editor-config.ts` holds the diff editor's construction options so they're unit-testable without a DOM/Monaco runtime (`main.ts` itself can't be imported standalone — it runs everything at module scope, including a `window.__TAURI__` access that throws outside Tauri). Fixed the editable-source-pane bug (`originalEditable: true`). Fixed the transparent-background bug (`background-color: Canvas` on `body`, the CSS4 system color that follows the OS theme). Root-caused and fixed the semantic-token coloring bug: `semanticHighlighting.enabled` must be set in `createDiffEditor`'s own construction options, not via a later `.updateOptions()` call on the constituent editors — Monaco's `DocumentSemanticTokensFeature` runs a one-time per-model gate check synchronously during construction, before any subsequent `updateOptions()` can take effect; found by isolating the Monaco+highlighting setup into a throwaway debug harness (no Tauri dependency) driven headlessly over the Chrome DevTools Protocol (no new npm dependency — Node's built-in `fetch`/`WebSocket`), since the real bug (main.ts's hard Tauri dependency) made even loading the page in a plain browser tab throw immediately. All four fixes re-verified visually in a real `cargo tauri dev` window (AppleScript-driven, per round 41's technique) after the code-level verification. `cargo test --workspace` green (`xoft-cli` +1 test), `clippy` clean, `tsc --noEmit`/`npm run build`/`npm test` clean |
